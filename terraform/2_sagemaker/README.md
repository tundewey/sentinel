# Stage 2 - SageMaker Embeddings

Deploys a SageMaker serverless endpoint for optional semantic incident retrieval.

## Steps
1. Copy `terraform.tfvars.example` to `terraform.tfvars`
2. Run `terraform init`
3. Run `terraform apply`
